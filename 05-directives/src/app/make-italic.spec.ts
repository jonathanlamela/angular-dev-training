import { MakeItalic } from './make-italic';

describe('MakeItalic', () => {
  it('should create an instance', () => {
    const directive = new MakeItalic();
    expect(directive).toBeTruthy();
  });
});
